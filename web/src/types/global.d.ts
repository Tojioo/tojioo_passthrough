export {};

declare global {
    const LiteGraph: any;
    type ISlotType = string | number;

    type LGraph = any;
    type LLink = any;
    type LGraphNode = any;

    // LiteGraph constants
    const INPUT: number;
    const OUTPUT: number;
}
